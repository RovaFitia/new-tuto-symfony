{% extends 'base.html.twig' %}

{% block title %}Toutes les recettes{% endblock %}

{% block body %}
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Sapiente qui voluptas laudantium saepe, culpa, similique, laborum est vero eius odio alias inventore nisi? Odit odio, amet iste vel debitis obcaecati.</p>
    <a href="{{ path('recipe.create') }}" class="btn btn-success">Ajouter une nouvelle recette</a>
    <table class="table table-bordered mt-3">
        <thead>
            <th>Nom de la recette</th>
            <th>Action</th>
        </thead>
        <tbody>
        {% for recipe in recipes %}
            <tr>
                <td>
                    <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">{{ recipe.title() }}</a>
                </td>
                <td>
                    <div class="d-flex gap-2">
                        <a href="{{ path('recipe.edit', {id: recipe.id}) }}" class="btn btn-sm btn-primary">Editer</a>
                        <form action="{{ path('recipe.delete', {id: recipe.id}) }}" method="post">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="btn btn-sm btn-danger">Supprimer</button>
                        </form>
                    </div>
                </td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}